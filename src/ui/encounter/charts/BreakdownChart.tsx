// module containing chart components where data (damage or healing) is broken down by source.

/**
 * Props accepted by the BreakdownChart component.
 */
type Props = {};

/**
 * The breakdown chart component - a chart where data (damage or healing) is broken down by source.
 *
 * @param props the props accepted by the BreakdownChart component.
 * @constructor
 */
const BreakdownChart = (props: Props) => {};
